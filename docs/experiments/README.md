# Experiment Records

Create one Markdown record per experiment family. State the question, hypothesis, independent variables, controlled variables, safety constraints, success metrics, baseline, ablations, and artifact run IDs. Link to committed configuration files instead of duplicating parameters.

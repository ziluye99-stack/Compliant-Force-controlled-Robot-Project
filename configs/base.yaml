experiment:
  name: compliant-force-control
  run_id: null
  seed: 42
  artifact_root: artifacts

runtime:
  mode: sim
  dry_run: false
  device: cpu

reproducibility:
  record_git_commit: true
  record_python_packages: true
  record_slurm_job_id: true

# Robot, sensor, controller, and model fields remain intentionally unset
# until a concrete experimental platform and safety protocol are specified.
robot: null
sensor: null
controller: null
model: null
